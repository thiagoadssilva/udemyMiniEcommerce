import React from 'react';
import ListarProdutos from '../ListarProdutos';

import {
    Container
} from './styled';

export default () => {
    return(
        <Container>
            <ListarProdutos />
        </Container>
    );
}